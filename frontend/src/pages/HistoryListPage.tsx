/**
 * History list page
 * 
 * TBD: Implements US3 history list functionality
 */

import { Card, Typography } from 'antd';

const { Title } = Typography;

export default function HistoryListPage() {
  return (
    <Card>
      <Title level={2}>历史评估记录</Title>
      <p>页面开发中...</p>
    </Card>
  );
}
