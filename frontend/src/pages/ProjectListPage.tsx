/**
 * Project list page
 * 
 * TBD: Implements project management functionality
 */

import { Card, Typography } from 'antd';

const { Title } = Typography;

export default function ProjectListPage() {
  return (
    <Card>
      <Title level={2}>项目管理</Title>
      <p>页面开发中...</p>
    </Card>
  );
}
