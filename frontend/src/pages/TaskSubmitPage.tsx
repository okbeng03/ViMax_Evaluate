/**
 * Task submission page
 * 
 * TBD: Implements US1 task submission functionality
 */

import { Card, Typography } from 'antd';

const { Title } = Typography;

export default function TaskSubmitPage() {
  return (
    <Card>
      <Title level={2}>提交评估任务</Title>
      <p>页面开发中...</p>
    </Card>
  );
}
